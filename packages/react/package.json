{
  "name": "@ownables/react",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "dependencies": {
    "@ownables/core": "workspace:*",
    "@ownables/platform-browser": "workspace:*",
    "@ownables/platform-viem": "workspace:*"
  },
  "peerDependencies": {
    "react": "^18 || ^19"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "clean": "rm -rf dist",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
